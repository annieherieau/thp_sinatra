<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Gossip Project</title>
</head>
<body>
  <header>
    <nav><ul>
      <li><a href="/">Voir tous les gossips</a></li>
      <li><a href="/gossips/new/">Ajouter un Gossip</a></li>
    </ul></nav>
    <h1>The Gossip Project</h1>
  </header>
  <!-- MAIN -->
  <main>
    <hr>

    <section>
    <h2>Voici la page du potin n°<%=  gossip_id %></h2>
    <p><strong>Son auteur :</strong> <%=  gossip.author %></strong></p>
    <p><strong>Le contenu du potin :</strong> <br>
      <%=  gossip.content %>
    </p>
    <p><a href="/gossips/<%=  gossip_id %>/edit">Modifier</a>  - <a href="/gossips/<%= gossip_id %>/suppr">Supprimer</a>  - <a href="/">Retour</a></p>
  </section>

    <hr>
    <section>
      <h3>Ajouter un commentaire</h3>
      <form action="/gossips/<%= gossip_id %>" method = "POST">
          <div>
            <label for="comment_author">*Saisis ton nom : </label> 
            <input required type="text" name ="comment_author" id="comment_author">
          </div>
          <div>
            <label for="comment_content">*Balance ton commentaire : </label><br>
            <textarea required name="comment_content" id="comment_content" cols="50" rows="2"></textarea>
          </div>
          <div>
            <input type="submit">
            <a href="/gossips/<%= gossip_id %>"><input type="button" value="Annuler"></a>
          </div>
          <small>* champs oblogatoires</small>
      </form>
    </section>
    <section>
      <h3>Tous les commentaires</h3>
      <% comments.each do |comment| %>
        <p><strong><%= comment.author %></strong> : <br>
        <%= comment.content %>
        </p>
        ---
      <% end %>
    </section>

  <hr>
  <!-- fin MAIN -->
  <footer>
    <div><a href="https://www.linkedin.com/in/annie-herieau/" target="_blank">Annie Hérieau</a> - Powered with Sinatra - <a href="https://github.com/annieherieau/thp_sinatra_the_gossip_project" target="blank">GitHub repo</a></div>
  </footer>
    
  </body>
  </html>